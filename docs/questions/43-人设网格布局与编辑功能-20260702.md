# 43 · 人设网格布局与编辑功能

**日期**：2026-07-02  
**类型**：UI / 人设

## 需求

- 去掉横滑长条选角区与顶栏「当前语气」
- 角色卡片改为网格，**新增角色**放在所有角色下方
- 详情页保留左右分栏，去掉左侧切换长列表
- 新增**编辑**功能

## 实现

### 主列表

- `persona-grid` 自适应网格展示角色卡
- 底部 `persona-create-section`：标题「新增角色」+ `PersonaTextImportForm` + 测试连通

### 详情页

- 顶栏下拉切换角色（替代左侧长列表）
- 新 Tab **编辑**：名称 / 来源 / 简介、Skill Markdown、结构化 JSON，保存与还原

### 后端

- `persona_update`：写入 manifest + `{id}.md` + `{id}.json`
