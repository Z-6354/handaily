# 99 · 构建禁用 MSI 仅保留 NSIS

**日期**：2026-07-04  
**标签**：构建、打包、NSIS

## 需求

以后构建不再生成 MSI 安装包，避免 WiX 下载超时且不需要双格式。

## 修改

`hanpet/src-tauri/tauri.conf.json`：

```json
"targets": ["nsis"]
```

（原为 `["nsis", "msi"]`）

## 构建

`npm run tauri:build` 仅产出 NSIS setup + 便携 exe，不再尝试 MSI。
