# 审计中优项继续优化

## 问题

继续处理 `docs/questions/54` 中未修复的中低优先级项。

## 修复摘要

### M4 — 崩溃孤儿 segment
- 新增 `db::recover_orphan_segments`：启动时将 open segment 闭合到当前时刻并计算 `duration_ms`
- 替换原先 `duration_ms = 0` 的 SQL

### M1 — DB 锁 poison 恢复
- `AppState::lock_db()` 统一封装 `lock_conn`
- IPC commands、coordinator、writer、poller、pet、period_scheduler 热路径改用 poison 恢复

### M3 — 分析队列满
- `coordinator` 队列满时 `eprintln` 日志

### M9 — 输入钩子失败
- `input_monitor` 钩子安装失败时打日志说明键鼠统计不可用

### M6 — 时间线筛选
- 切换 30m/1h/2h 且不在第一页时自动 `load(0)`
- 筛选区增加提示文案

### M8 — 桌宠设置回滚
- 省电模式保存失败回滚本地 state
- 角色大小保存失败 `refreshStatus` 恢复

### L1 — 桌宠窗口创建互斥
- `PetRuntimeState.window_create_lock` 防并发重复创建

## 相关文件

- `hanpet/src-tauri/src/db/mod.rs`、`state.rs`
- `hanpet/src-tauri/src/analysis/coordinator.rs`、`period_scheduler.rs`
- `hanpet/src-tauri/src/tracker/writer.rs`、`poller.rs`、`input_monitor.rs`
- `hanpet/src-tauri/src/pet/mod.rs`、`ipc/commands.rs`
- `hanpet/src/pages/TimelineView.tsx`、`hanpet/src/pages/PetPanel.tsx`

## 仍未处理（可选下一轮）

- M5：共享 Tokio runtime（AI adapter）
- 时段 AI 失败日志/ surfaced to UI
- 分析 worker 退出 join
- 时间线服务端时间范围查询
