# 审计低优项收尾（第四轮）

## 问题

继续处理审计剩余低优先级项 L2–L5 与时间线 UX。

## 修复摘要

### L2 — 桌宠调度器 `scheduler_running` 复位
- 调度 async 任务用 `Drop` guard，退出/ panic 时 `scheduler_running = false`，可再次 `ensure_remark_scheduler`

### L3 — 时间线占位按钮
- 移除 disabled 的编辑/删除按钮，避免像 Bug

### L4 — pet 事件 listen 清理
- `setupPetEvents` 幂等 + 保存 `unlisten`；Vite HMR `dispose` 时解除订阅

### L5 — 截图 AI 失败可观测
- 新增 `vision::execute_or_fallback`，失败 `eprintln` 后回退本地洞察
- `coordinator` 同步使用

### 附加 — 时间线错误文案
- `TimelineView` 使用 `parseApiError`，与设置页一致

## 相关文件

- `hanpet/src-tauri/src/pet/mod.rs`
- `hanpet/src-tauri/src/analysis/vision.rs`、`coordinator.rs`
- `hanpet/src/pet/main.ts`
- `hanpet/src/pages/TimelineView.tsx`

## 审计清单状态

高/中/低优项均已处理；后续为功能增强（时间线编辑/删除、更多 UI  polish）。
