# 138 · 项目垃圾清理与体积优化

**日期**：2026-07-07

## 背景

`src-tauri/target` 约 **49 GB**（debug 40G + release 6G + release-fast 3G 历史遗留）。

## 清理项

| 项 | 大小 | 处理 |
|----|------|------|
| `target/release-fast/` | ~2.8 GB | 已删除（122 前遗留 profile） |
| 根目录 `*.log` | 小 | 已删除 `live2d-import.log` |
| `target/debug/` | ~40 GB | 可选，`npm run clean:debug`（需先关应用） |
| 全量 `cargo clean` | ~50 GB | `npm run clean:all` |

## 新增命令

```powershell
npm run clean          # 安全清理（release-fast + 日志）
npm run clean:debug    # 另删 debug/（下次 dev 重编译）
npm run clean:all      # cargo clean 全部
```

脚本：`scripts/clean-project.ps1`

## 代码优化

- 删除未使用的 `to_info` / `skin_infos`
- 修复 `useInfiniteScroll`、`characterRoster` 未使用 import
- `.gitignore` 增加 `live2d-import.log`

## 相关

- [137-Cargo-target目录重复修复](137-Cargo-target目录重复修复-20260707.md)
