# 123 · 项目架构整洁化与文档整合

**日期**：2026-07-06  
**标签**：重构、日志、文档、清理

## 需求

优化项目架构：删除冗余日志与临时文件，保持代码整洁，整合文档入口。

## 清理项

| 类型 | 处理 |
|------|------|
| `AzurLaneSD-master/` | 删除（~1600 文件参考资源，应用已用 `public/assets/pet/`） |
| `tmp-*`、`*.log`、plan 草稿、`skel_header.txt` | 删除 |
| `scripts/test-spine36-*.mjs` | 删除（一次性 Spine 调试脚本） |

## 代码

| 项 | 说明 |
|----|------|
| `hanpet/src-tauri/src/log.rs` | 统一 `warn`（发布保留）/ `info`（仅 debug） |
| 各模块 `eprintln!` | 迁移至 `log::warn` / `log::info`；移除成功路径日志 |
| `pet_log` IPC | 删除（前端未使用） |
| `hanpet/src/pet/` | 移除 `console.warn`，保留关键 `console.error` |

## 测试

保留 Rust 内联单元测试（`npm run test:rust`），未删除业务测试模块。

## 文档

| 文件 | 作用 |
|------|------|
| `docs/README.md` | **文档总入口**（架构、构建、问答主题索引） |
| `README.md` | 精简为快速启动 + 指向 docs |
| `docs/questions/README.md` | 补全 80–84 索引行 |

## 相关

- [07 · 功能板块与代码结构总览](./07-功能板块与代码结构总览-20260702.md)
- [121 · 目录说明](./121-src与src-tauri及release构建目录说明-20260706.md)
