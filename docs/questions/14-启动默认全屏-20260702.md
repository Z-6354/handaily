# 14 · 启动默认全屏（最大化）

**日期**：2026-07-02  
**类型**：体验优化

## 需求

应用启动时默认全屏显示。

## 实现

Windows 桌面场景采用**最大化**（保留标题栏与任务栏，符合日报助手类应用习惯）：

- `tauri.conf.json`：`"maximized": true`
- `lib.rs` setup：创建窗口后调用 `win.maximize()`

从托盘再次打开时保持用户上次窗口大小，不强制最大化。

📁 已归档：`docs/questions/14-启动默认全屏-20260702.md`
