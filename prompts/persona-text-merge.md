用户提供了**补充文本**，请合并进已有角色结构化 JSON。原则同预处理：**不丢语义、不编造、轻量归类**。

已有 JSON：
```json
{{existing_json}}
```

补充文本：
```
{{new_text}}
```

只输出合并后的完整 JSON 对象（字段名不变）。补充内容与已有字段冲突时，以补充文本为准；可合并的数组合并去重；`extra` 可增补键值。

不要 markdown 代码块，不要其它说明。
