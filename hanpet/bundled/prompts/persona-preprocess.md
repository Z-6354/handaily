将以下角色相关的大段复合文本，做**最轻量**的结构化整理。原则：

- **不丢语义**：原文信息尽量保留，不要概括过头或编造
- **不扩写**：不要添加文本里没有的内容
- **只做归类**：把自然语言拆到对应字段

字段说明：
- `name`：角色名
- `source`：作品/出处（若文本未提及可留空）
- `introduction`：介绍、背景、设定摘要
- `personality`：性格要点（字符串数组，每条一句）
- `speech_style`：说话风格、语气、口癖习惯
- `sample_lines`：台词、代表性对白（字符串数组，尽量原文摘录）
- `relationships`：与他人的关系
- `taboos`：禁忌、不会做的事、雷区（数组，无则 `[]`）
- `extra`：其它不好归类但重要的信息（键值对，如 `外貌`、`能力`）

参考角色名：{{name}}
参考出处：{{source}}

原始文本：
```
{{raw_text}}
```

只输出一个 JSON 对象，不要 markdown 代码块，不要其它说明。字段名必须与上文一致；数组字段即使为空也输出 `[]`，`extra` 无内容时输出 `{}`。

**输出长度限制（必须遵守，确保 JSON 完整闭合）**：
- `introduction` 不超过 500 字
- `personality` 最多 10 条
- `sample_lines` 最多 8 条，每条不超过 120 字
- `extra` 最多 5 个键
- 优先保留核心设定，次要细节可合并省略
